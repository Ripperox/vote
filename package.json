{
  "name": "dao-governance",
  "version": "1.0.0",
  "description": "On-chain DAO governance dApp — Solidity contracts (DAO + GovernanceToken) with a static ethers.js frontend.",
  "main": "app.js",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "test:e2e": "playwright test",
    "deploy:sepolia": "hardhat run scripts/deploy.js --network sepolia",
    "start": "serve . -p 3000"
  },
  "keywords": [
    "dao",
    "governance",
    "ethereum",
    "solidity",
    "hardhat",
    "ethers"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "ethers": "^6.13.4",
    "web3": "^4.14.0"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@playwright/test": "^1.60.0",
    "hardhat": "^2.22.17",
    "serve": "^14.2.6"
  }
}
